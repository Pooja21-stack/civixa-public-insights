/**
 * useMockApi — toggle this to false once the real FastAPI backend is running.
 * While true, every API call returns rich mock data with a simulated network delay.
 */
export const USE_MOCK_API = true;
